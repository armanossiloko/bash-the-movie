<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:BTM.MobileApp.ViewModels"
             mc:Ignorable="d"
             x:Class="BTM.MobileApp.Views.LoginPage"
             Title="Login">
    <ContentPage.BindingContext>
        <viewModels:LoginViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Padding="5" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
            <!--<Label Text="Welcome to Bash the Movie!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />-->
            <Image x:Name="Logo" Source="logo.png" WidthRequest="200"></Image>

            <Label Text="Username:"></Label>
            <Entry Text="{Binding Username}"></Entry>

            <Label Text="Password:"></Label>
            <Entry Text="{Binding Password}" IsPassword="True"></Entry>

            <Button Text="Login" HorizontalOptions="FillAndExpand" Command="{Binding LoginCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>